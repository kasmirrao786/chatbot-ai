[deploy]
cmd = "node -e \"require('dotenv').config();const fs=require('fs');const db=require('./lib/db');db.query(fs.readFileSync('./db/schema.sql','utf8')).then(()=>{console.log('schema ok');return db.getPool().end();}).catch(e=>{console.error('SCHEMA APPLY FAILED:',e.message);process.exit(1);});\" && node scripts/migrate-tenants-to-db.js && node server.js"

